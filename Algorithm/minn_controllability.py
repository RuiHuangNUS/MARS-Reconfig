from quadrotor_graph import Quadrotor_graph
import numpy as np
import CM
dir_expression = np.array([[-1, 0],[0, 1],[1, 0],[0, -1]])
glb_G = []


def traverse(curr_num, num_limit):
    x=1

def find_adjacent_units(n, faulty_idx):
    En = []
    traverse_list = [faulty_idx]

    # begin with n = 3 with priori knowledge


def calc_minn_ctrl_assmbly(G):
    idx = int(np.where(G.is_working == 0)[0])
    num = G.n
    glb_G = G
    traverse_list = [idx]

    # for i in range (num):   # number of units to be calculated


